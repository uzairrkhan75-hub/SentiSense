# Google Sign-In (OAuth client ID from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=

# Backend API base URL. Leave empty for local dev (uses the Vite proxy to
# localhost:5000). In production, point this at your API subdomain, e.g.
# https://api.yourdomain.com/api
VITE_API_BASE_URL=
